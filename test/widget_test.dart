import 'package:flutter_test/flutter_test.dart';

// Placeholder test — full widget tests require Supabase to be mocked.
// CI builds are validated via flutter analyze; runtime tests run on device.
void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
